# Aura server environment — copy this to .env and fill in real values
# .env is gitignored and never committed

# PostgreSQL password — used by both the db service and the api service
POSTGRES_PASSWORD=change_me_in_production

# HMAC salt for API key hashing — change this before first production deploy
AURA_HMAC_SALT=change_me_in_production
